# JWT Secret for token signing (change this in production!)
JWT_SECRET=super-secret-key

# Database Connection URL
DB_URL=postgres://usr:test123@postgres:5432/books?sslmode=disable

# RabbitMQ Configuration (optional, uses defaults in docker-compose)
# RABBITMQ_DEFAULT_USER=guest
# RABBITMQ_DEFAULT_PASS=guest

# Service URLs (for internal Docker network)
# USER_SERVICE_URL=http://user-service:8002
# BOOK_SERVICE_URL=http://book-service:8000
# ORDER_SERVICE_URL=http://order-service:8081

# Production Security Note:
# For production deployment, please:
# 1. Change JWT_SECRET to a strong, random value
# 2. Update database credentials
# 3. Configure proper SSL/TLS certificates
# 4. Set up proper firewall rules
